name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to server
        run: |
          sshpass -p 'O5izCcjbB49oqukh' ssh -o StrictHostKeyChecking=no root@185.8.172.52 'cd /root/nest-ci-cd && git pull && npm install && npm run build && sudo systemctl restart nginx'
